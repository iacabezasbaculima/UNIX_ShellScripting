#!/bin/sh
# The grep command is used to:
# 1. Search in files for certain words
# 2. Search in files by regular expression

# Search for "root" in the dummydata text file
# "^" is used to find only rows starting with "root"
grep "^root" dummydata

